Python dependencies : 
* pyexcel
* pyexcel-odsr for ods, fods files support

Run `pip3 install pyexcel pyexcel-odsr` to install dependencies.

Run `python3 convert.py -h` for detailed usage information.